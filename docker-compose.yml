version: '3.8'
services:
  web:
    container_name: web
    build:
      context: .
    volumes:
      - .:/usr/src/quasar/web_backend
    ports:
      - "8081:8081"
    tty: true
    command: "yarn quasar dev"
